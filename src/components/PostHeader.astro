---
// library imports
import { Image } from "@astrojs/image/components";
// utils imports
import { slugify, formatDate } from "../js/utils";
const { category, title, author, date, image } = Astro.props;
---

<header>
  <div class="container" data-aos="fade-in" data-aos-delay="250" data-aos-duration="300">
    <small>
      <a href={`/category/${slugify(category)}/`} class="badge">{category}</a>
    </small>
    <h2 class="h2">{title}</h2>
    <p>
      by <a href={`/author/${slugify(author)}/`}>{author}</a> • {
        formatDate(date)
      }
    </p>
  </div>
  <Image
    src={image.src}
    alt={image.alt}
    width="1200"
    height="600"
    format="avif"
    fit="cover"
    quality={80}
    aspectRatio="5:2"
    class="hero-image"
  />
</header>
